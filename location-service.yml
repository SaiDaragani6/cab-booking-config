spring:
  application:
    name: LOCATION-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/cab_booking_location?createDatabaseIfNotExist=true
    username: root
    password: cab_booking
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8086

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    path: /api/locations/v3/api-docs
  swagger-ui:
    path: /api/locations/swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha

